VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
VITE_CAPTCHA_SITE_KEY="0x4AAAAAACp-HoE7EM2wCB8i"
VITE_SENTRY_DSN=
VITE_APP_VERSION=

# Runtime policy (enable in staging after baseline smoke, then production)
# VITE_RUNTIME_CAPABILITY_ENFORCE=true

# Operational readiness (scripts only — never expose in client)
# SUPABASE_TARGET=auto   # auto | local | remote (auto prefers local Docker when running)
# Run: npm run supabase:sync-env  (writes .env.local from `supabase status -o json`)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=
TENANT_ID=

# Supabase Edge Functions
APP_ORIGINS=
REMINDER_CRON_SECRET=
EVENT_DELIVERY_WORKER_SECRET=
SENTRY_DSN=
OUTBOUND_HEALTHCHECK_URLS=
RATE_LIMIT_BACKEND=upstash
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# E2E (Playwright)
E2E_BASE_URL=
E2E_SUPER_ADMIN_EMAIL=
E2E_SUPER_ADMIN_PASSWORD=
E2E_ADMIN_EMAIL=
E2E_ADMIN_PASSWORD=
E2E_CLINIC_SLUG=
E2E_DOCTOR_NAME=
E2E_PORTAL_EMAIL=
E2E_PORTAL_PASSWORD=
