# Runtime chaos scenarios

Executable matrix lives in `src/platform/runtime/coordination/__tests__/chaosRuntimeMatrix.test.ts` (Vitest).

Expand with Playwright for multi-tab races (`tenant switch during workflow`, `barrier timeout`, etc.).
